apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "odf-ibm.fullname" . }}-test-connection"
  labels:
{{ include "odf-ibm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "odf-ibm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
